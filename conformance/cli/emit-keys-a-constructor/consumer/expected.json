{
  "description": "The key only the rule can produce, read back by the reader a user's ESLint runs: `new Wrapper(size)` resolves across the `.d.ts` boundary to the constructor the manifest colored.",
  "diagnostics": []
}
