{
  "description": "What the emitted manifest buys a consumer: `twice` and `fetchOk` are clean, the terminal `.catch(h)` on the async one bridges, `widen`'s one entry answers for both overloads, `each`'s condition discharges against a clean callback and reports against a throwing one, and `register`'s positively-recorded `[]` keeps a stored callback unconditioned.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 36,
      "column": 3,
      "endLine": 36,
      "endColumn": 7,
      "messageId": "carriedConditionArgumentThrowing",
      "message": "Call to `each` escapes this `@nothrow` function: the carrier that colors it declares it non-throwing given `visit`, and the argument passed for `visit` is throwing — its body was analyzed and can throw. Your outs: bridge this call with `try`/`catch`, or pass something non-throwing for `visit`."
    }
  ]
}
