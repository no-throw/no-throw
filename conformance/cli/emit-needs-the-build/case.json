{
  "description": "A manifest hashes what is about to be published, so emit runs after the build: with no output on disk there is nothing to hash, and a manifest without `files` would make every consumer's staleness check vacuous.",
  "steps": [
    {
      "emit": [],
      "expect": "refused",
      "names": ["./dist/index.js", "after the build"]
    },
    { "absent": "nothrow.json" }
  ]
}
