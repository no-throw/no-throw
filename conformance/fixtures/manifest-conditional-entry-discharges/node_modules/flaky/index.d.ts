export interface Repo {
  save(item: string): void;
}

export declare function eachItem(
  items: readonly string[],
  cb: (item: string) => void,
): void;

export declare function saveAll(items: readonly string[], repo: Repo): void;
