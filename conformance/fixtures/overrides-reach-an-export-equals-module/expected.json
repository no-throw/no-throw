{
  "description": "The CommonJS shape, `declare const x: Colors` behind `export =`: the checker reports no exports at all for such a module, so it published nothing and no key of any spelling could reach it. The module *is* the exported value, so what its type carries are its published names — `paints.red` keyed `red`, exactly as `export const red` would have been.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 10,
      "column": 10,
      "endLine": 10,
      "endColumn": 27,
      "messageId": "unbridgedCall",
      "message": "Call to `paints.boom` escapes this `@nothrow` function: it is colored `throwing` by the carrier that answers for it — a shipped manifest, an overlay, an override, or the standard-library baseline — so calling it can throw. Your outs, in precedence order: bridge this call with `try`/`catch`; assert the color in `nothrow.overrides.json`; install or write an `@no-throw/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`."
    }
  ]
}
