{
  "description": "`String(o)` coerces its argument, so the argument's conversion member is consulted at the call. The call to `String` itself floors bodyless until the baseline colors it, which is why two diagnostics land on this line rather than one.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 19,
      "column": 10,
      "endLine": 19,
      "endColumn": 16,
      "messageId": "unbridgedCall"
    },
    {
      "file": "src/index.ts",
      "line": 19,
      "column": 17,
      "endLine": 19,
      "endColumn": 25,
      "messageId": "inferredThrowingHiddenTransfer",
      "message": "Coercing `stringly` to a primitive escapes this `@nothrow` function: it runs the method `toString`, whose body was analyzed and can throw. Your outs: bridge it with `try`/`catch`, or make the method `toString` non-throwing — mark it `@nothrow` and the escapes inside it are reported too."
    }
  ]
}
