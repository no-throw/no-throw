{
  "description": "An offer endorses the bridge it writes, so it is made only where somebody proved the throw. `JSON.parse` carries a baseline entry colored `throwing` — fuzzed evidence, and the bridge really is the remedy — so the edit is offered. `WeakRef.prototype.deref` ships with its color absent rather than asserted, because the gate cannot construct a `WeakRef` to probe: the throw there is this tool's assumption, and the reader cannot change it from here, so one click to wrap it would make swallowing the path of least resistance over the largest source of floors there is. Both messages still name the bridge first; only one presses it.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 3,
      "column": 10,
      "endLine": 3,
      "endColumn": 26,
      "messageId": "unbridgedCall",
      "suggestions": [
        {
          "desc": "Bridge this with `try`/`catch`.",
          "output": [
            "/** @nothrow */",
            "export function parse(text: string): unknown {",
            "  try {",
            "    return JSON.parse(text);",
            "  } catch {}",
            "}",
            "",
            "/** @nothrow */",
            "export function read(reference: WeakRef<object>): object | undefined {",
            "  return reference.deref();",
            "}",
            ""
          ]
        }
      ]
    },
    {
      "file": "src/index.ts",
      "line": 8,
      "column": 10,
      "endLine": 8,
      "endColumn": 27,
      "messageId": "unbridgedCall",
      "message": "Call to `reference.deref` escapes this `@nothrow` function: it is declared in a `lib.*.d.ts` the shipped standard-library baseline has no entry for, and that baseline is the only carrier able to reach one, so nothing colors it and it is assumed to throw. Your outs: bridge this call with `try`/`catch`; or, if it cannot throw, report it against the shipped standard-library baseline at https://github.com/MidnightDesign/no-throw/issues — the other three carriers are keyed by npm package name, and no key in that grammar reaches a `lib.*.d.ts` member.",
      "suggestions": []
    }
  ]
}
