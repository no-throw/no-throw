{
  "description": "Module evaluation is outside the color model: a top-level throw, an imported module's side effect, a static field initializer and a `static {}` block all run before any function does, so a function's color says nothing about them.",
  "diagnostics": []
}
