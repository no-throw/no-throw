{
  "description": "The editor's half of the contract: a floored sync call offers the bridge as an edit the reader accepts, wrapping the statement it escapes from and nothing else. Asserted as the file the edit produces, because a bridge is only a bridge in the right place. Wrapping a `return` is offered too, and deliberately leaves a compiler error — the bridge is complete, and what is left is the one thing no tool can decide.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 6,
      "column": 3,
      "endLine": 6,
      "endColumn": 10,
      "messageId": "unbridgedCall",
      "message": "Call to `risky` escapes this `@nothrow` function: it is declared without a body — an ambient declaration, a `.d.ts`, or a value known only by its function type — and no mark, manifest, overlay or override colors it, so it is assumed to throw. Your outs, in precedence order: bridge this call with `try`/`catch`; assert the color in `nothrow.overrides.json`; install or write an `@nothrow/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`.",
      "suggestions": [
        {
          "desc": "Bridge this with `try`/`catch`.",
          "output": [
            "declare function risky(): void;",
            "declare function count(): number;",
            "",
            "/** @nothrow */",
            "export function usesRisky(): void {",
            "  try {",
            "    risky();",
            "  } catch {}",
            "}",
            "",
            "/** @nothrow */",
            "export function countsRisky(): number {",
            "  return count();",
            "}",
            ""
          ]
        }
      ]
    },
    {
      "file": "src/index.ts",
      "line": 11,
      "column": 10,
      "endLine": 11,
      "endColumn": 17,
      "messageId": "unbridgedCall",
      "message": "Call to `count` escapes this `@nothrow` function: it is declared without a body — an ambient declaration, a `.d.ts`, or a value known only by its function type — and no mark, manifest, overlay or override colors it, so it is assumed to throw. Your outs, in precedence order: bridge this call with `try`/`catch`; assert the color in `nothrow.overrides.json`; install or write an `@nothrow/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`.",
      "suggestions": [
        {
          "desc": "Bridge this with `try`/`catch`.",
          "output": [
            "declare function risky(): void;",
            "declare function count(): number;",
            "",
            "/** @nothrow */",
            "export function usesRisky(): void {",
            "  risky();",
            "}",
            "",
            "/** @nothrow */",
            "export function countsRisky(): number {",
            "  try {",
            "    return count();",
            "  } catch {}",
            "}",
            ""
          ]
        }
      ]
    }
  ]
}
