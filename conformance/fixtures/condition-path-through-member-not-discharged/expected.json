{
  "description": "The path is discharged at the member the body actually enters: a repo whose `save` can throw fails the condition, and the message names the path rather than the parameter.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 16,
      "column": 3,
      "endLine": 16,
      "endColumn": 27,
      "messageId": "conditionArgumentThrowing",
      "message": "Call to `saveAllB` escapes this `@nothrow` function: it is non-throwing given `repo.save`, and the argument passed for `repo.save` is throwing — its body was analyzed and can throw. `saveAllB` enters `repo.save` at src/index.ts:11. Your outs: bridge this call with `try`/`catch`, or pass something non-throwing for `repo.save`."
    }
  ]
}
