{
  "description": "The same call is judged on the callback actually passed: a throwing one fails the condition, and the message names the parameter, where `myEach` enters it, and the outs.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 12,
      "column": 3,
      "endLine": 12,
      "endColumn": 9,
      "messageId": "conditionArgumentThrowing",
      "message": "Call to `myEach` escapes this `@nothrow` function: it is non-throwing given `cb`, and the argument passed for `cb` is throwing — its body was analyzed and can throw. `myEach` enters `cb` at src/index.ts:7. Your outs: bridge this call with `try`/`catch`, or pass something non-throwing for `cb`."
    }
  ]
}
