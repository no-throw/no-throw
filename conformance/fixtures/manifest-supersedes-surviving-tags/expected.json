{
  "description": "A valid manifest supersedes tags package-wide: a tag the manifest contradicts is not honored, and a tag on a symbol the manifest does not mention is not honored either — per-key fallback would resurrect through a comment the lying mark emit refused to write down.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 5,
      "column": 10,
      "endLine": 5,
      "endColumn": 25,
      "messageId": "unbridgedCall",
      "message": "Call to `safeParse` escapes this `@nothrow` function: it carries a `@nothrow` tag that its package's own `nothrow.json` supersedes — a valid manifest answers for the whole package — and that manifest has no entry for it. Your outs, in precedence order: bridge this call with `try`/`catch`; assert the color in `nothrow.overrides.json`; install or write an `@nothrow/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`."
    },
    {
      "file": "src/index.ts",
      "line": 10,
      "column": 3,
      "endLine": 10,
      "endColumn": 9,
      "messageId": "unbridgedCall",
      "message": "Call to `boom` escapes this `@nothrow` function: it is colored `throwing` by the carrier that answers for it — a shipped manifest, an overlay or an override — so calling it can throw. Your outs, in precedence order: bridge this call with `try`/`catch`; assert the color in `nothrow.overrides.json`; install or write an `@nothrow/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`."
    }
  ]
}
