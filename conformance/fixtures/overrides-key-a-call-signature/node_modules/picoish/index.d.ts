export interface Formatter {
  (input: string): string;
}

export interface Risky {
  (input: string): string;
}

export interface Colors {
  red: Formatter;
  bold: Formatter;
  parse: Risky;
}

declare const picoish: Colors;
export default picoish;
