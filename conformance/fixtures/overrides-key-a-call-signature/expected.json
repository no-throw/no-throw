{
  "description": "The picocolors shape, which is the one the key grammar exists for: a call enters the *signature*, so where a member's type carries it — `red: Formatter` — the property is not on the way. `Formatter#()` colors `pc.red` and `pc.bold` at once, because both properties are that one signature. `Colors#parse` is the guess a reader makes instead, and it is written here to be seen doing nothing: `pc.parse` floors with the property colored, since nobody ever enters it.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 15,
      "column": 10,
      "endLine": 15,
      "endColumn": 18,
      "messageId": "unbridgedCall",
      "message": "Call to `pc.parse` escapes this `@nothrow` function: it is declared without a body — an ambient declaration, a `.d.ts`, or a value known only by its function type — and no mark, manifest, overlay, override or baseline entry colors it, so it is assumed to throw. Your outs, in precedence order: bridge this call with `try`/`catch`; assert the color in `nothrow.overrides.json`; install or write an `@no-throw/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`."
    }
  ]
}
