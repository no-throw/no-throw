export function untilUndefined(limit) {
  if (limit === undefined) return;
  throw new RangeError(String(limit));
}

export function untilNullish(limit) {
  if (limit === undefined || limit === null) return;
  throw new RangeError(String(limit));
}
