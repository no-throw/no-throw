{
  "description": "A condition says calling a parameter is clean; it has no form for the promise that call hands back, and none for a chain handler. Both floor, the same way consuming a conditioned producer's iterator does.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 10,
      "column": 10,
      "endLine": 10,
      "endColumn": 25,
      "messageId": "unbridgedAwait",
      "message": "Awaiting `produce()` escapes this `@nothrow` function: it is produced by a call to one of this function's own parameters, and a condition can say that calling a parameter is clean but not that the promise it hands back never rejects. Your outs, in precedence order: bridge it with `try { await … } catch`; assert the color in `nothrow.overrides.json`; install or write an `@nothrow/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`."
    },
    {
      "file": "src/index.ts",
      "line": 17,
      "column": 10,
      "endLine": 17,
      "endColumn": 39,
      "messageId": "unbridgedAwait",
      "message": "Awaiting `resolves().then(handle)` escapes this `@nothrow` function: a handler in the chain is reached through a parameter of this function, and a chain handler is not something a call site can discharge. Your outs, in precedence order: bridge it with `try { await … } catch`; assert the color in `nothrow.overrides.json`; install or write an `@nothrow/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`."
    }
  ]
}
