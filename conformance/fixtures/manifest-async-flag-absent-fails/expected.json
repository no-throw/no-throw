{
  "description": "#15's counterfactual, with the flag removed: the same consumer code fails, because absence must mean \"might sync-throw\" and the `.d.ts` surface alone can never license the `.catch` bridge.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 10,
      "column": 3,
      "endLine": 10,
      "endColumn": 15,
      "messageId": "unbridgedCall",
      "message": "Call to `fetchFlaky` escapes this `@nothrow` function: it is colored `throwing` by the carrier that answers for it — a shipped manifest, an overlay, an override, or the standard-library baseline — so calling it can throw. Your outs, in precedence order: bridge this call with `try`/`catch`; assert the color in `nothrow.overrides.json`; install or write an `@nothrow/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`."
    }
  ]
}
