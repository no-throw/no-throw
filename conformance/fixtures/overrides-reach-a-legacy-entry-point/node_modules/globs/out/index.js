const Globs = (source) => Promise.resolve([source].flat());

Globs.sync = (source) => [source];
Globs.escapePath = (source) => source.replace(/[*?]/g, "\\$&");
Globs.posix = {
  escapePath: (source) => {
    throw new Error("posix escaping is not implemented");
  },
};

module.exports = Globs;
