declare function Globs(source: string): Promise<string[]>;
declare function Globs(source: string[]): Promise<string[]>;

declare namespace Globs {
  function sync(source: string): string[];
  function escapePath(source: string): string;
  namespace posix {
    function escapePath(source: string): string;
  }
}

export = Globs;
