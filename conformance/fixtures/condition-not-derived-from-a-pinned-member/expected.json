{
  "description": "A parameter typed `string` pins which function `startsWith` is — no value of that type can put anything else there — so the body enters a member the baseline already colors and derives no condition there is nothing to defer. Callers pass what they like: a literal, a parameter, an expression. A number, a bigint, a symbol and a parameter constrained to `string` pin the same way.",
  "diagnostics": []
}
