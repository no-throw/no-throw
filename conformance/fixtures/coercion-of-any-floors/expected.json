{
  "description": "`any` says nothing about what runs, so both coercing it and reading a member off it floor — with a message that says the checker could not resolve it and names the outs in precedence order.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 5,
      "column": 13,
      "endLine": 5,
      "endColumn": 18,
      "messageId": "unbridgedHiddenTransfer",
      "message": "Coercing `loose` to a primitive escapes this `@nothrow` function: the checker cannot resolve `loose` to a declaration, so nothing can say whether a body runs here. Your outs, in precedence order: bridge this call with `try`/`catch`; assert the color in `nothrow.overrides.json`; install or write an `@nothrow/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`."
    },
    {
      "file": "src/index.ts",
      "line": 10,
      "column": 10,
      "endLine": 10,
      "endColumn": 21,
      "messageId": "unbridgedHiddenTransfer",
      "message": "Reading `loose.value` escapes this `@nothrow` function: the checker cannot resolve `loose.value` to a declaration, so nothing can say whether a body runs here. Your outs, in precedence order: bridge this call with `try`/`catch`; assert the color in `nothrow.overrides.json`; install or write an `@nothrow/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`."
    }
  ]
}
