{
  "description": "`any` and `unknown` say nothing about what runs, so coercing one, reading a member off one, and using one as a computed key — which applies ToPropertyKey and can reach `toString` — all floor, with a message that says the checker could not resolve it and names the outs in precedence order.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 7,
      "column": 13,
      "endLine": 7,
      "endColumn": 18,
      "messageId": "unbridgedHiddenTransfer",
      "message": "Coercing `loose` to a primitive escapes this `@nothrow` function: the checker cannot resolve `loose` to a declaration, so nothing can say whether a body runs here. Your outs, in precedence order: bridge this call with `try`/`catch`; assert the color in `nothrow.overrides.json`; install or write an `@nothrow/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`."
    },
    {
      "file": "src/index.ts",
      "line": 12,
      "column": 13,
      "endLine": 12,
      "endColumn": 19,
      "messageId": "unbridgedHiddenTransfer",
      "message": "Coercing `opaque` to a primitive escapes this `@nothrow` function: the checker cannot resolve `opaque` to a declaration, so nothing can say whether a body runs here. Your outs, in precedence order: bridge this call with `try`/`catch`; assert the color in `nothrow.overrides.json`; install or write an `@nothrow/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`."
    },
    {
      "file": "src/index.ts",
      "line": 17,
      "column": 10,
      "endLine": 17,
      "endColumn": 21,
      "messageId": "unbridgedHiddenTransfer",
      "message": "Reading `loose.value` escapes this `@nothrow` function: the checker cannot resolve `loose.value` to a declaration, so nothing can say whether a body runs here. Your outs, in precedence order: bridge this call with `try`/`catch`; assert the color in `nothrow.overrides.json`; install or write an `@nothrow/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`."
    },
    {
      "file": "src/index.ts",
      "line": 22,
      "column": 17,
      "endLine": 22,
      "endColumn": 22,
      "messageId": "unbridgedHiddenTransfer",
      "message": "Coercing `loose` to a primitive escapes this `@nothrow` function: the checker cannot resolve `loose` to a declaration, so nothing can say whether a body runs here. Your outs, in precedence order: bridge this call with `try`/`catch`; assert the color in `nothrow.overrides.json`; install or write an `@nothrow/*` overlay; or, if you own the package, ship a manifest with `nothrow emit`."
    }
  ]
}
