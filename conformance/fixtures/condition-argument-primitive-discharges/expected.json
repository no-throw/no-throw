{
  "description": "A condition on a member of a primitive parameter is discharged, not deferred forever: a `string` has no subtype to override `startsWith`, so the type names the entry the baseline colors however the argument was written — a literal, a template, a member, a reassignable binding, a parameter constrained to `string`. And it is a consultation rather than a pass: the same discharge against `String#repeat` reports.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 46,
      "column": 10,
      "endLine": 46,
      "endColumn": 15,
      "messageId": "conditionArgumentFloored",
      "message": "Call to `twice` escapes this `@nothrow` function: it is non-throwing given `s.repeat`, and the argument passed for it is colored `throwing` by the shipped standard-library baseline, so calling it can throw. `twice` enters `s.repeat` at src/index.ts:12. Your outs: bridge this call with `try`/`catch`; or, if it cannot throw, report it against the shipped standard-library baseline at https://github.com/no-throw/no-throw/issues — the other three carriers are keyed by npm package name, and no key in that grammar reaches a `lib.*.d.ts` member."
    }
  ]
}
