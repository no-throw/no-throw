{
  "description": "A `static` field is the same declaration with a different key — `Service.handle` rather than `Service#handle` — so it binds on the same footing as an instance field.",
  "diagnostics": [
    {
      "file": "src/index.ts",
      "line": 4,
      "column": 5,
      "endLine": 4,
      "endColumn": 18,
      "messageId": "uncaughtThrow"
    }
  ]
}
